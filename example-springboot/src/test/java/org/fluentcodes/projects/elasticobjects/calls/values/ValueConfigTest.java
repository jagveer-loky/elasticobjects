package org.fluentcodes.projects.elasticobjects.calls.values;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.junit.Test;

/**
 * Created by werner.diwischek on 11.03.18.
 */
public class ValueConfigTest {
    private static final Logger LOG = LogManager.getLogger(ValueConfigTest.class);

/*
    @Test
    public void fromSourceCsv() throws Exception {
        TestHelper.printStartMethod();
        ValueConfig cache = (ValueConfig) TestObjectProvider.EO_CONFIGS_CACHE.find(ValueConfig.class, "calc");
        Assert.assertNull(cache.getValue());
        Assert.assertEquals("calc", cache.getValueKey());
        AssertEO.compare(TestObjectProvider.EO_CONFIGS_CACHE, cache);
    }
 */
}
