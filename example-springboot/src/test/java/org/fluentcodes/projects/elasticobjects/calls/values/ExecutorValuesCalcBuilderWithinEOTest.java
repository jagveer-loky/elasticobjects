package org.fluentcodes.projects.elasticobjects.calls.values;

/**
 * Created by werner.diwischek on 11.03.18.
 */
public class ExecutorValuesCalcBuilderWithinEOTest {
/*
    @Test
    public void fromCalc_WITH_PathAdd() throws Exception {
        TestHelper.printStartMethod();
        EO adapter = TestObjectProvider.create();

        final Map attributes = new HashMap();
        attributes.put(ExecutorListTemplate.EXECUTE, "ValuesMisc.set(calc)");
        attributes.put("calc", "1+2");
        attributes.put(EO_STATIC.F_PATH, "addList");

        CallExecutor callExecutor = new CallExecutor(attributes);
        adapter.addCall(callExecutor);
        AssertEO.compare(adapter);
        adapter.executeCalls();
        Assert.assertEquals(3.0, adapter.get("/addList/result"));
    }

 */
}
