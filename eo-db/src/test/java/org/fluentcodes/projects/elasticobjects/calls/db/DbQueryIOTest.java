package org.fluentcodes.projects.elasticobjects.calls.db;

/**
 * Created by Werner on 28.1.2018.
 */
public class DbQueryIOTest {
    /*
    @Test
    public void queryAnObject()  {

        DbQueryConfig dbQueryConfig = (DbQueryConfig) TestObjectProvider.EO_CONFIGS_CACHE.find(DbQueryConfig.class, DQ_H2_MEM_BASIC_BASIC_TEST);
        Assert.assertNotNull(dbQueryConfig);
        DbQueryIO dbQueryIO = new DbQueryIO(dbQueryConfig);
        List row = dbQueryIO.readRow();
        int counter = 0;
        while (row != null) {
            Assert.assertNotNull(row);
            row = dbQueryIO.readRow();
            counter++;
        }
        Assert.assertEquals(2, counter);
    }

    @Test
    public void queryASubObject()  {

        DbQueryConfig dbQueryConfig = (DbQueryConfig) TestObjectProvider.EO_CONFIGS_CACHE.find(DbQueryConfig.class, DQ_H2_MEM_BASIC_SUB_TEST);
        Assert.assertNotNull(dbQueryConfig);
        DbQueryIO dbQueryIO = new DbQueryIO(dbQueryConfig);
        List row = dbQueryIO.readRow();
        int counter = 0;
        while (row != null) {
            Assert.assertNotNull(row);
            row = dbQueryIO.readRow();
            counter++;
        }
        Assert.assertEquals(2, counter);
    }

     */
}

