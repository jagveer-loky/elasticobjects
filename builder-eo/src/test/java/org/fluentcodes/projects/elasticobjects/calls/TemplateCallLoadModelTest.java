package org.fluentcodes.projects.elasticobjects.calls;

import org.fluentcodes.projects.elasticobjects.TestBuilderEOProvider;
import org.junit.Assert;
import org.junit.Test;

public class TemplateCallLoadModelTest {
/*
    @Test
    public void executeJavaControl1Tpl()  {
        EO adapter = TestCallsProvider.executeTemplateActionForAdapter(TestBuilderEOProvider.MAP_CONTROL, "JavaControl1.tpl");
        Assert.assertNotNull(adapter.get("builder/data"));
        Assert.assertEquals("BuilderTest", adapter.get("builder/data/BuilderTest/naturalId"));
        //AssertEO.compare(adapter);
    }

    @Test
    public void executeJavaControl2Tpl()  {
        EO adapter = TestCallsProvider.executeTemplateActionForAdapter(TestBuilderEOProvider.MAP_CONTROL, "StaticModelsFromXlsx.tpl");
        Assert.assertNotNull(adapter.get("models"));
        Assert.assertEquals("Call", adapter.get("models/Call/naturalId"));
        //AssertEO.compare(adapter);
    }

    @Test
    public void executeJavaControl3Tpl()  {
        EO adapter = TestCallsProvider.executeTemplateActionForAdapter(TestBuilderEOProvider.MAP_CONTROL, "StaticFieldsFromXlsx.tpl");
        Assert.assertNotNull(adapter.get("fields"));
        Assert.assertEquals("testString", adapter.get("fields/testString/naturalId"));
        //AssertEO.compare(adapter);
    }

 */
}
